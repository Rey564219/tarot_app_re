This folder includes an older Node.js API skeleton used for early prototyping.

Current source of truth for the API is the FastAPI implementation in `backend/`.
Keep the skeleton for reference only unless we explicitly decide to migrate back.
